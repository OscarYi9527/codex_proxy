# runtime state
proxy.log
proxy-err.log
.proxy.pid
current-model.json

# per-session files (window-specific, not shared)
sessions/

# no machine-specific exclusions — launch scripts use portable paths
