{
  "name": "codex-proxy",
  "version": "2.4.0",
  "type": "module",
  "description": "Multi-upstream model routing proxy for Codex — ChatGPT subscription, OpenAI API, and DeepSeek",
  "main": "index.js",
  "bin": {
    "codex-proxy": "src/server.js"
  },
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check index.js",
    "test": "node --test tests/test-codex-proxy.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "codex",
    "proxy",
    "openai",
    "chatgpt",
    "deepseek",
    "model-router"
  ],
  "license": "MIT",
  "dependencies": {
    "undici": "^8.7.0"
  }
}
